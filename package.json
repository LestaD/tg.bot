{
  "name": "tgbot",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node build/index.js",
    "build": "tsc --outDir build",
    "dev": "nodemon -w lib/ -x \"rm -rf build && npm run build && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sergey Sova <i.am@lestad.net> (https://lestad.top)",
  "license": "GPL-3.0",
  "dependencies": {
    "is-promise": "^2.1.0",
    "typescript": "^2.0.9"
  },
  "devDependencies": {
    "@types/is-promise": "^0.0.3",
    "@types/node": "^6.0.46",
    "nodemon": "^1.11.0"
  }
}
